import 'package:flutter/widgets.dart';

import '../../base_widget.dart';

class FMenuDivider extends BaseWidget {
  const FMenuDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('FMenuDivider'),
    );
  }
}
